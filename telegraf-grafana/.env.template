# Security Cloud Control Firewall Manager (SCCFM) Configuration
# Copy this file to .env and fill in the values

# InfluxDB admin token (used for both InfluxDB setup and Telegraf authentication). Set to a secure value of your choice.
INFLUXDB_TOKEN=my-secret-influx-token

# Tenant configuration is now in tenants.json
# Edit tenants.json to add your tenants with their name, region, fmc_uid, and bearer_token
